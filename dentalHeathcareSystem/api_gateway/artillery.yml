config:
  target: "http://localhost:3000"
  phases: 
    - duration: 60
      arrivalRate: 10

scenarios: 
  - name: "get appointments"
    flow:
      - get: 
          url: "/api/appointments/get/available/appointments"